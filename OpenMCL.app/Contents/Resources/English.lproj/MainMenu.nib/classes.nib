{
    IBClasses = (
        {
            ACTIONS = {
                backtrace = id; 
                continue = id; 
                exitBreak = id; 
                inspect = id; 
                interrupt = id; 
                newListener = id; 
                restarts = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}